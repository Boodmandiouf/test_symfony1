{% extends 'base.html.twig' %}

{% block body %}
    <h1>Myentity</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ myentity.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('myentity_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('myentity_edit', { 'id': myentity.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
